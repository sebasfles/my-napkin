---
name: i18n
model: sonnet
paths:
  - app/src/**/*.tsx
  - app/src/messages/*.json
reference: docs/conventions/i18n.md
---

# i18n

Every user-facing string in the changed lines goes through `useTranslations` or `getTranslations`; none is a literal in JSX text, `aria-label`, `placeholder`, `title`, `alt` or toast and dialog copy.
Every translation key used in the diff exists in both `app/src/messages/es.json` and `app/src/messages/en.json`.
After the change, `es.json` and `en.json` have the same key set; a key added to one is added to the other.
Keys added in the diff follow `{page or component}.{element}`, as the reference states.
Dates and numbers in the diff are formatted with `useFormatter`, never with `toLocaleString`, direct `Intl.*` calls or string concatenation.
Where the diff renders the Excalidraw component, it passes a `langCode` prop derived from the active locale.
Report as clean: diagram names and other user content, `data-testid` values, log messages, developer-facing error messages, files under `app/tests/`, and files under `app/src/components/ui/` as generated by the shadcn CLI.
